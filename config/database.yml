default: &default
   adapter: postgresql
   encoding: unicode

development:
   adapter: postgresql
   encoding: unicode
   database: friends
   username: postgres
   password: password

test:
   adapter: postgresql
   encoding: unicode
   database: friends
   username: postgres
   password: password

production:
<<: *default
adapter: postgresql
database: db/production.sqlite3

